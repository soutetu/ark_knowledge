<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>Knowledge</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag    "application", media: "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">
    <div class="row">
      <div class="col-md-12">
        <div class="jumbotron">
          <h1>Knowledge</h1>
          <p>Knowledge sharing of limited extent.</p>
          <p><a class="btn btn-inverse" href="#login_modal" data-toggle="modal">Login</a></p>
        </div>
      </div>
    </div>
  </div>

  <div id="login_modal" class="modal fade">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <a href="javascript:void(0)" class="close" data-dismiss="modal">&times;</a>
          <h4 class="modal-title">ログイン</h4>
        </div>

        <div class="modal-body">
          <%= form_tag(session_create_path) do %>
            <div class="form-group">
              <label class="sr-only control-label">メールアドレス</label>
              <%= email_field_tag :email, nil, class: "form-control", placeholder: "メールアドレス", maxlength: 100, required: true %>
            </div>

            <div class="form-group">
              <label class="sr-only control-label">パスワード</label>
              <%= password_field_tag :password, nil, class: "form-control", placeholder: "パスワード", required: true %>
            </div>

            <%= submit_tag "ログイン", class: "btn btn-primary" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>

  <%= render partial: "shared/footer" %>
</body>
</html>